#include<stdio.h>

int fib(int x);

int main(){
    int n=0;
    for (int i = 0; i < n; i++)
    {
        /* code */
    }
    

    
    return 0;
}

int fib(int x){
    return fib(x-1) + fib(x-2);
}